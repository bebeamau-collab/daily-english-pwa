# 測試結果

測試日期：2026-07-24

## 自動化核心測試：7 / 7 通過

- 字庫共 300 筆、12 個指定主題，所有欄位完整。
- 每個單字都有兩組不同的英文例句與繁體中文翻譯，共 600 組例句。
- 兩組例句皆包含目標單字或片語，可正確粗體標示並交給語音功能朗讀。
- 300 筆單字與片語都有 KK 音標，且已正確連結至單字資料。
- 每筆例句都含可粗體顯示的目標字或片語。
- 固定種子每日選字可重現，同一天結果相同。
- 連續 30 天輪完 300 筆前沒有重複。
- 完成每日學習後，10 筆紀錄會保存在 localStorage，第一輪排在隔日。
- 「記得」依序排入 3、7、14、30 天後，最後標示精通。
- 「忘記」重設為第一階段並排入隔日。
- streak 連續計算與中斷歸零符合預期。
- 女聲／男聲偏好可經 JSON 儲存與還原，並正確對應 Bella／Michael 靜態 MP3 路徑。
- 自然語音檔無法取得時才會退回裝置預設美式語音，且備用語音音高固定為原始值 `1`。

## 實際瀏覽器測試：通過

- 375×812：今日 10 字正常顯示，完成按鈕、成功訊息與 streak 正常。
- 重新整理：完成狀態與 10 筆已學紀錄仍存在。
- 隔日複習：紅色徽章與待複習數均為 10。
- 翻卡：翻面前僅將英文設為可讀答案，翻面後顯示中文與雙語例句。
- 選「記得」：本日佇列由 10 變 9。
- 選「忘記」：本日佇列由 9 變 8，隔日重新出現。
- 進度頁：完成天數、複習中、精通、streak 與學過清單一致。
- 812×375 橫向、768×1024 平板：沒有水平溢位，底部分頁未遮住內容。
- PWA 基本資源：HTML、CSS、JavaScript、manifest、service worker 與兩個 PNG 圖示皆回傳 200。
- 圖示尺寸：180×180 與 512×512 PNG 正確。
- 離線：關閉本機伺服器後重新整理，APP 仍載入，10 張單字卡可用。
- 瀏覽器主控台：0 個錯誤。

## 本次音標與發音功能檢查：通過

- 今日單字卡與複習卡都有 KK 音標。
- 每張今日單字卡與複習卡都有獨立的美式發音按鈕。
- 今日單字卡會顯示「例句 1／例句 2」，兩句都有獨立播放按鈕。
- 複習卡翻面後會顯示兩組雙語例句，並顯示兩個完整例句播放按鈕。
- 女聲／男聲切換按鈕皆符合至少 44×44px 的手機觸控範圍，並有鍵盤焦點與 `aria-pressed` 狀態。
- Kokoro 自動產音工作完整通過；Bella 900 個、Michael 900 個，共 1,800 個 MP3。
- 已抽查兩種聲音的第一個與最後一個檔案，四個檔案均存在 GitHub 且具有獨立 blob SHA。
- GitHub Pages 最終發布工作成功；Bella 與 Michael 的正式 MP3 網址均可載入，不再回傳 404。
- 在正式 APP 內分別播放 `meal` 的 Bella 與 Michael 音檔，兩次播放按鈕的 `aria-pressed` 都正確變為 `true`。
- 備用裝置語音速度設為適合學習的 0.86 倍，語系固定為 `en-US`，不再變更音高模擬性別。
- `speech.js`、`phonetics.js`、`second-examples.js` 已加入 service worker 的離線快取清單。
- 同來源 MP3 第一次成功播放後會由 service worker 的 GET 快取流程保存，供之後離線重播。
- HTML、CSS、所有 JavaScript、manifest、service worker 與兩個 PNG 圖示，共 12 個 PWA 基本資源均回傳 HTTP 200。

> 正常情況會播放固定的 Bella／Michael Kokoro 美式聲音；只有自然語音檔載入失敗時才會使用裝置內建語音。

## 清理

瀏覽器測試用 localStorage 已清除；最後確認完成天數 0、複習中 0、精通 0、學過單字 0。
